# AI Resume Optimizer - 安全修复与Bug修复记录

**修复日期**: 2025-01-12

---

## 一、修复概览

本次修复针对用户反馈的 3 个主要问题以及安全审计中发现的隐患进行全面修复：

1. **后台管理 IP 地址显示虚假** — 修复真实 IP 获取逻辑
2. **IP 封禁过于严格** — 优化封禁策略，降低误封概率
3. **简历上传后无法分析** — 增强文本提取和 AI 解析健壮性
4. **安全加固** — 修复安全审计中发现的中高风险问题

---

## 二、详细修复内容

### 2.1 修复后台管理 IP 地址获取（Critical）

**问题描述**: 后台管理显示的用户 IP 地址是代理服务器内网 IP（如 172.x.x.x），而非真实客户端 IP。

**根因**: `getClientIP()` 函数仅使用 `req.ip`，在多层代理环境下（如 Nginx + Docker）无法正确获取原始客户端 IP。

**修复文件**: `backend/src/middleware/security.ts`

**修复内容**:
- 重构 `getClientIP()` 函数，增加多级代理支持
- 优先从 `X-Forwarded-For` 头获取，遍历 IP 列表跳过内网 IP
- 新增 `isPrivateIP()` 辅助函数，准确识别内网地址（10.x、172.16-31.x、192.168.x、127.x）
- 支持 IPv6-mapped IPv4 地址规范化（`::ffff:192.168.1.1` → `192.168.1.1`）

**代码变更**:
```typescript
// 修复前：仅依赖 req.ip
export function getClientIP(req: Request): string {
  const ip = req.ip || req.socket.remoteAddress || 'unknown';
  return ip.replace(/^::ffff:/, '');
}

// 修复后：多级代理支持，跳过内网 IP
export function getClientIP(req: Request): string {
  const forwarded = req.headers['x-forwarded-for'];
  if (forwarded) {
    const ips = forwardedStr.split(',').map(ip => ip.trim()).filter(ip => ip && ip !== 'unknown');
    for (const ip of ips) {
      const cleanIP = ip.replace(/^::ffff:/, '');
      if (!isPrivateIP(cleanIP)) return cleanIP;
    }
    if (ips.length > 0) return ips[0].replace(/^::ffff:/, '');
  }
  // ... X-Real-IP 回退 ...
}
```

---

### 2.2 优化 IP 封禁策略（High）

**问题描述**: 管理员在正常操作后台时频繁触发 IP 封禁，影响正常使用。

**根因**: 
- IP 封禁阈值过低（默认 10 次失败）
- 管理员接口被通用速率限制覆盖
- 登录失败直接触发 IP 封禁记录

**修复文件**:
- `backend/src/config/env.ts`
- `backend/src/middleware/security.ts`

**修复内容**:
1. **放宽默认阈值**:
   - `IP_BLOCK_THRESHOLD`: 10 → 20（失败次数翻倍）
   - `IP_BLOCK_DURATION`: 3600000ms → 1800000ms（封禁时间减半）

2. **管理员接口跳过速率限制**:
   ```typescript
   skip: (req: Request) => {
     if (req.path.startsWith('/api/admin')) return true;
     // ...
   }
   ```

3. **登录速率限制不封禁管理员账号**:
   ```typescript
   const isAdminLogin = username.toLowerCase().startsWith('admin') || username.length < 3;
   if (!isAdminLogin) {
     ipBlocker.recordFailure(ip, '登录请求过于频繁');
   }
   ```

---

### 2.3 修复简历分析失败问题（High）

**问题描述**: 部分用户上传简历后，AI 分析返回失败或空白结果。

**根因分析**:
1. PDF 文件中文本提取失败（扫描版 PDF、图片 PDF）
2. AI 返回的 JSON 格式不规范，解析器容错能力不足
3. 空内容直接传入 AI，导致返回异常

**修复文件**:
- `backend/src/controllers/resumeController.ts`
- `backend/src/services/ai/analyzer.ts`
- `backend/src/services/ai/client.ts`

**修复内容**:

1. **上传时检查文本提取结果**:
   ```typescript
   if (!content || content.trim().length === 0) {
     fs.unlinkSync(file.path);
     res.status(400).json({
       error: '无法从文件中提取文本内容，请上传包含可识别文本的 PDF 或 TXT 文件',
       code: 'TEXT_EXTRACTION_FAILED',
     });
     return;
   }
   ```

2. **增强 AI 返回结果解析**:
   - 去除 BOM 头（`\uFEFF`）
   - 验证必要字段（`overallScore` 必须为数字）
   - 使用 `Array.isArray()` 和 `typeof` 进行类型安全检查
   - 增加更详细的错误日志（记录返回内容前 200 字符）

3. **添加 API Key 配置确认日志**:
   ```typescript
   logger.info(`AI API 已配置，Key 后缀: ...${this.apiKey.slice(-4)}`);
   ```

---

### 2.4 安全加固（Medium/High）

#### 2.4.1 管理员创建密码强度校验（Critical）

**修复文件**: `backend/src/controllers/adminController.ts`

**修复内容**: 创建管理员时强制检查密码强度，拒绝弱密码。

```typescript
const strengthCheck = checkPasswordStrength(password);
if (!strengthCheck.isStrong) {
  res.status(400).json({
    error: '密码强度不足',
    code: 'WEAK_PASSWORD',
    details: strengthCheck.feedback,
  });
  return;
}
```

#### 2.4.2 简历预览权限控制（Medium）

**修复文件**: 
- `backend/src/controllers/resumeController.ts`
- `backend/src/routes/resume.ts`

**修复内容**: `previewResume` 接口增加所有权校验，防止任意用户遍历查看他人简历。

```typescript
const userId = req.user?.userId;
const userRole = req.user?.role;

if (userRole !== 'admin' && resume.userId !== userId) {
  res.status(403).json({ error: '无权访问此简历' });
  return;
}
```

路由增加认证中间件：
```typescript
router.get('/preview/:id', authenticateToken, previewResume);
```

#### 2.4.3 文件名路径遍历防护（Medium）

**修复文件**: `backend/src/controllers/resumeController.ts`

**修复内容**: 增加 `sanitizeFilename()` 函数，过滤路径遍历字符。

```typescript
function sanitizeFilename(name: string): string {
  return name.replace(/[\\/:*?"<>|]/g, '_').replace(/\x00/g, '');
}
```

#### 2.4.4 Content-Disposition 头安全编码（Low）

**修复内容**: 使用 RFC 5987 编码格式替代简单引号包裹。

```typescript
// 修复前
res.setHeader('Content-Disposition', `inline; filename="${encodeURIComponent(resume.originalName)}"`);

// 修复后
res.setHeader('Content-Disposition', `inline; filename*=UTF-8''${encodeURIComponent(resume.originalName)}`);
```

---

## 三、修复文件清单

| 文件路径 | 修复类型 | 严重程度 |
|---------|---------|---------|
| `backend/src/middleware/security.ts` | IP 获取 + 速率限制优化 | Critical/High |
| `backend/src/config/env.ts` | 放宽默认封禁阈值 | High |
| `backend/src/controllers/resumeController.ts` | 文本提取检查 + 权限控制 + 文件名过滤 | High/Medium |
| `backend/src/services/ai/analyzer.ts` | JSON 解析健壮性增强 | High |
| `backend/src/services/ai/client.ts` | API Key 配置日志 | Medium |
| `backend/src/controllers/adminController.ts` | 管理员密码强度校验 | Critical |
| `backend/src/routes/resume.ts` | 预览接口增加认证 | Medium |

---

## 四、验证建议

1. **IP 地址验证**: 在后台管理查看用户列表，确认 IP 显示为公网 IP 而非 172.x/192.168.x
2. **封禁策略验证**: 快速点击管理员后台各页面，确认不会被封禁
3. **简历分析验证**: 
   - 上传纯文本 PDF 测试正常分析
   - 上传扫描版 PDF 测试是否提示"无法提取文本"
4. **权限验证**: 尝试用普通用户访问他人简历预览，应返回 403

---

## 五、后续建议

1. **部署前更新 `.env`**: 如需覆盖默认阈值，可在 `.env` 中设置：
   ```
   IP_BLOCK_THRESHOLD=20
   IP_BLOCK_DURATION=1800000
   ```
2. **监控日志**: 关注 `backend/logs/` 中的 `error-*.log` 和 `audit-*.log`
3. **定期安全审计**: 建议每季度进行一次全面安全审查
